/**
 * @constant {number} DEFAULT_GRID_SIZE - The default grid size.
 * @default 16
 */
export const DEFAULT_GRID_SIZE = 16;

/**
 * @constant {number} DEFAULT_GRID_SIZE_IN_PIXELS - The default grid size in pixels.
 * @default 1000
 */
export const GRIDS_CONTAINER_SIZE_IN_PIXELS = 1000;
